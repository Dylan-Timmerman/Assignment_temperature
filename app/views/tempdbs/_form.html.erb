<%= form_with(model: tempdb, local: true) do |form| %>
  <% if tempdb.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(tempdb.errors.count, "error") %> prohibited this tempdb from being saved:</h2>

      <ul>
      <% tempdb.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :temp %>
    <%= form.text_field :temp, id: :tempdb_temp %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
